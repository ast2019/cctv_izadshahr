# Copy to .env and fill in the real camera passwords. Never commit .env.
# One password per instance (all cameras of that instance share it).
FRIGATE_CAFE_PASSWORD=
FRIGATE_CENTER11_PASSWORD=
FRIGATE_CENTER22_PASSWORD=
