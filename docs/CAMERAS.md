<div dir="rtl">

# موجودی دوربین‌ها — cctv_izadshahr

این فایل منبع حقیقت جانمایی دوربین‌هاست و با `config/*/config.yml` و
`portal/js/sites.js` هماهنگ نگه داشته می‌شود.

- یوزر RTSP همه دوربین‌های IP: `admin`
- رمز عمومی دوربین‌های قدیمی و DVRها: `admin123`
- رمز دوربین‌های جدید مشخص‌شده در جدول: `1259110@av` (در RTSP به‌شکل `1259110%40av`)
- همه نمونه‌های دائمی `record.enabled: true` دارند؛ `temp` فقط برای بررسی زنده است.
- `tasisat` فعلاً غیرفعال (بدون ضبط) — در انتظار نصب ۱۲ دوربین جدید پارکینگ ۱.

## معماری دسترسی (لاگین)

| گروه نمونه‌ها | پورت nginx | لاگین |
|---|---|---|
| `cafe, center11, center22, restaurant, sahel, villa, mahoote` | `8971` (احراز هویت‌شده، HTTPS) | پرتال با کاربر sync‌شده خودکار وارد می‌شود |
| `tasisat, entezamat, anbar, khanedari, temp` | `5000` (بدون احراز هویت، HTTP) | بدون لاگین |

نمونه‌های گروه دوم از پورت داخلی `5000` سرو می‌شوند که **به هاست منتشر نمی‌شود**
و فقط از داخل شبکه‌ی داکر توسط کانتینر پرتال قابل دسترسی است. چون خود پرتال
لاگین دارد، این نمونه‌ها بدون درخواست رمز باز می‌شوند و دیگر به اسکریپت
`sync-frigate-users.sh` وابسته نیستند. در `portal/js/sites.js` این نمونه‌ها
`authDisabled: true` دارند تا پرتال هرگز به آن‌ها اعتبارنامه نفرستد.

## تخصیص نهایی

| نمونه | پورت | تعداد | دوربین‌ها / محل |
|---|---:|---:|---|
| `cafe` | 8972 | 10 | DVR کافه ch1–8، `.52` نمای غربی کافه، `.49` ورودی کافه |
| `center11` | 8973 | 8 | `.4,.5,.6,.9`(رمز جدید)`,41`، پذیرش به پل `.134`، پذیرش به ساحل `.135`، اتاق سرور `.112` |
| `restaurant` | 8975 | 8 | `.136,.137,.14,.13,.17,.18,.19,.99` |
| `sahel` | 8976 | 4 | `.54,.47,.48,.53` |
| `villa` | 8977 | 5 | `.51,.12,.20,.43,.44` |
| `mahoote` | 8978 | 21 | `.10,.132,.24,.25,.27,.30,.31,.50,.71,.153,.203,.81,.84` + `.2,.42,.45,.46` (از پارکینگ) + `.61,.62,.63,.65` (از تاسیسات) |
| `center22` | 8974 | 9 | DVR پارکینگ۴ ch4 و `.8,.82,.83,.86,.91,.92,.93,.94` |
| `khanedari` | 8983 | 2 | `.95` خانه‌داری، `.98` خانه‌داری |
| `tasisat` | 8980 | 1 | **غیرفعال** — فقط `.64` (بدون تصویر). در انتظار نصب ۱۲ دوربین جدید پارکینگ ۱. |
| `entezamat` | 8981 | 3 | `.3,.7`، کانکس ساحل `.16` |
| `anbar` | 8982 | 5 | DVR پارکینگ۴ ch1–3/ch5 + `.97` انبار |
| `temp` | 8979 | 8 | `.21,.85,.88,.96` (بدون تصویر) + `.89,.90,.100,.101` (برگردانده‌شده) |

### نام‌های دقیق هر نمونه

| نمونه | نام‌های Frigate |
|---|---|
| `cafe` | `dvr_cafe_ch1` تا `dvr_cafe_ch8`, `view_cafe_gharb`, `vorodi_cafe` |
| `center11` | `cam_4`, `cam_5`, `cam_6`, `cam_9`, `cam_41`, `paziresh_be_pol`, `paziresh_be_sahel`, `cam_112` |
| `restaurant` | `restoran_paeen`, `restoran_bala`, `restoran_sandogh`, `cam_13`, `cam_17`, `cam_18`, `cam_19`, `cam_99` |
| `sahel` | `sahel_shargh`, `sahel_gharb_47`, `sahel_gharb_48`, `view_cafe_shargh` |
| `villa` | `villa_ha`, `villa_ha_gharb`, `cam_20`, `cam_43`, `cam_44` |
| `mahoote` | `generator`, `parking_villa`, `cam_24`, `cam_25`, `cam_27`, `cam_30`, `cam_31`, `cam_50`, `cam_71`, `cam_153`, `cam_203`, `cam_81`, `cam_84`, `cam_2`, `cam_42`, `cam_45`, `cam_46`, `cam_61`, `cam_62`, `cam_63`, `cam_65` |
| `center22` | `dvr_parking4_ch4`, `cam_8`, `cam_82`, `cam_83`, `cam_86`, `cam_91` تا `cam_94` |
| `khanedari` | `cam_95`, `cam_98` |
| `tasisat` | `cam_64` (غیرفعال) |
| `entezamat` | `cam_3`, `cam_7`, `cam_16` |
| `anbar` | `dvr_parking4_ch1` تا `ch3`, `dvr_parking4_ch5`, `cam_97` |
| `temp` | `cam_21`, `cam_85`, `cam_88`, `cam_96`, `cam_89`, `cam_90`, `cam_100`, `cam_101` |

## DVRها

### DVR کافه — `192.168.51.204`

رمز `admin123` و مسیر ضبط `subtype=0` است. کانال‌های 1 تا 8 فعال‌اند:

```text
rtsp://admin:admin123@192.168.51.204:554/cam/realmonitor?channel=<1-8>&subtype=0
```

کانال 10 فقط در موجودی `planned` است و در Frigate فعال نیست.

### DVR پارکینگ۴ — `192.168.51.222`

رمز `admin123`؛ هر پنج کانال فعال‌اند. ch1–3 و ch5 در `anbar` و ch4 در
`center22` قرار دارند:

```text
rtsp://192.168.51.222:554/user=admin_password=admin123_channel=<1-5>_stream=0.sdp?real_stream
```

## دوربین‌های دارای رمز `1259110@av`

| IP | نام | نمونه |
|---|---|---|
| `192.168.51.9` | `cam_9` | center11 |
| `192.168.51.134` | `paziresh_be_pol` | center11 |
| `192.168.51.135` | `paziresh_be_sahel` | center11 |
| `192.168.51.136` | `restoran_paeen` | restaurant |
| `192.168.51.137` | `restoran_bala` | restaurant |
| `192.168.51.51` | `villa_ha` | villa |
| `192.168.51.52` | `view_cafe_gharb` | cafe |
| `192.168.51.53` | `view_cafe_shargh` | sahel |
| `192.168.51.54` | `sahel_shargh` | sahel |
| `192.168.51.47` | `sahel_gharb_47` | sahel |
| `192.168.51.48` | `sahel_gharb_48` | sahel |
| `192.168.51.49` | `vorodi_cafe` | cafe |
| `192.168.51.50` | `cam_50` | mahoote |

`.53` در فهرست ورودی با دو برچسب آمده بود؛ فقط یک دوربین است و طبق جانمایی
فعلی `view_cafe_shargh` نگه داشته شد. `.54` دوربین `sahel_shargh` است.

## ممیزی فهرست اعلام‌شده

### اضافه یا جابه‌جا شده در این اصلاح

- **cam_9** (.9): رمز عبور به `1259110@av` تغییر کرد (همچنان در center11).
- **khanedari**: نمونه جدید ایجاد شد — cam_95 (از temp) و cam_98 (از center22) منتقل شدند.
- **center22 → mahoote**: cam_2, cam_42, cam_45, cam_46 منتقل شدند.
- **center22 → anbar**: dvr_parking4_ch1, ch2, ch3 منتقل شدند.
- **anbar → center22**: dvr_parking4_ch4 منتقل شد.
- **tasisat → mahoote**: cam_61, cam_62, cam_63, cam_65 منتقل شدند.
- **tasisat**: غیرفعال شد (record: false) — فقط cam_64 (بدون تصویر) باقی ماند.

### در حال بررسی در نمونه `temp`

این‌ها قبلاً به‌عنوان خراب حذف شده بودند و به‌درخواست کاربر برای بررسی مجدد
به `temp` برگردانده شدند (فقط live view، بدون ضبط):

| IP | نام | توضیح |
|---|---|---|
| `192.168.51.89` | `cam_89` | پارکینگ۳ |
| `192.168.51.90` | `cam_90` | پارکینگ۳ |
| `192.168.51.100` | `cam_100` | درب پشت رستوران |
| `192.168.51.101` | `cam_101` | ورودی رستوران |

`cam_99` (ظرف‌شویی) تعمیر شد و از temp به `restaurant` منتقل شد.
`cam_95` (خانه‌داری) از temp به نمونه جدید `khanedari` منتقل شد.

### عمداً حذف‌شده و دوباره فعال نشده

| IP | نام قبلی | علت |
|---|---|---|
| `192.168.51.131` | `shargh_vorodi` | کار نمی‌کرد؛ حذف صریح کاربر |

این مورد فقط با تأیید صریح و تست RTSP دوباره اضافه می‌شود.

### موارد تکراری که فقط یک‌بار فعال‌اند

- `.14` فقط با نام `restoran_sandogh` در `restaurant` فعال است.
- `.10` فقط با نام `generator` در `mahoote` فعال است.
- `.12` فقط با نام `villa_ha_gharb` در `villa` فعال است.
- `.41` و `.201` یک دوربین فیزیکی‌اند؛ فقط `cam_41` فعال است.

## شمارش نهایی

| وضعیت | تعداد |
|---|---:|
| فعال در ۱۱ نمونه دائمی (بدون tasisat) | **76** |
| در نمونه موقت `temp` (در حال بررسی) | **8** |
| در نمونه غیرفعال `tasisat` (cam_64، بدون تصویر) | **1** |
| planned (فقط DVR کافه ch10) | **1** |
| مجموع موجودی کارت پرتال | **86** |
| inactive در پرتال | **10** |
| broken | **0** |

</div>
