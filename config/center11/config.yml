# =============================================================================
# center11 — SKELETON (NVR-only pattern). Fill in real cameras, then:
#   docker compose up -d frigate-center11
# Shared password: FRIGATE_RTSP_PASSWORD (in .env). Username/IP go inline.
# =============================================================================
mqtt:
  enabled: false

detect:
  enabled: false

record:
  enabled: true
  retain:
    days: 2
    mode: all

snapshots:
  enabled: false

birdseye:
  enabled: false

ffmpeg:
  global_args: -hide_banner -loglevel warning
  output_args:
    record: preset-record-generic

# Fill these in. One go2rtc stream + one record-only camera per channel.
go2rtc:
  streams: {}
  #  cam1:
  #    - rtsp://admin:{FRIGATE_RTSP_PASSWORD}@192.168.1.10:554/cam/realmonitor?channel=1&subtype=0

cameras: {}
  #  cam1:
  #    ffmpeg:
  #      inputs:
  #        - path: rtsp://127.0.0.1:8554/cam1
  #          input_args: preset-rtsp-restream
  #          roles: [record]

version: 0.16-0
